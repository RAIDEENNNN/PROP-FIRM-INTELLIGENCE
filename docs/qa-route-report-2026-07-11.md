# FundedScope QA Route Report — 2026-07-11

Scope: desktop and mobile route/button audit based on the current local build. Production-only flows still require the deployed API URL, Supabase JWT secret, and applied Supabase migrations before live verification.

## Build verification

- `npm run build:api` — passed.
- `npm run build:web` — passed.
- `npm run typecheck` — passed.

## Route audit

| Route | Desktop state | Mobile state | Button/control state |
| --- | --- | --- | --- |
| `/` | Builds successfully; homepage sections render. | Responsive build passes. | Header, CTAs, search, notification bell render. Notification bell uses DB when configured, otherwise fallback notices. |
| `/sign-up` | Supabase signup form wired. | Responsive form layout. | Creates Supabase Auth user, stores Supabase session token when available, upserts profile when email confirmation is not required. |
| `/sign-in` | Supabase login form wired. | Responsive form layout. | Login stores Supabase session token. Password reset button calls Supabase reset email. |
| `/profile` | Trader Hub builds. | Responsive build passes. | Profile save uses Supabase session token first, then legacy token fallback. |
| `/dashboard` | Builds successfully. | Responsive build passes. | Dashboard remains public/demo until authenticated user-specific widgets are connected to production tables. |
| `/prop-firms` | Builds successfully. | Responsive build passes. | Static prop firm directory still present on frontend until production API is deployed and Supabase rows are seeded. |
| `/prop-firms/[slug]` | 25 SSG profile routes build. | Responsive build passes. | Bookmark now persists to API for signed-in users when `NEXT_PUBLIC_API_URL` is configured; local fallback for guests. Report button links to `/report`. Recently viewed writes to API when configured. |
| `/compare` | Builds successfully. | Responsive build passes. | Public API comparison responses now strip database IDs and internal fields. Frontend still uses local data until API replacement is completed. |
| `/brokers` | Builds successfully with expanded broker directory. | Responsive build passes. | Broker logos use favicon fallback; spread fields remain source-gated where not verified. |
| `/spreads` | Builds successfully. | Responsive build passes. | Spread data remains source-safe; numeric values should only be shown when official/feed-backed/manual verified. |
| `/decision-engine` | Builds successfully. | Responsive build passes. | Language changed to “Options worth researching based on your preferences.” It shows limitations, availability warning, verification date, and data completeness warning. |
| `/report` | Builds successfully. | Responsive form layout. | Real report form posts to `/api/persistence/information-reports`; if API URL is missing it shows a polished configuration message. |
| `/admin` | Builds successfully. | Responsive build passes. | Admin operations map documents CRUD/moderation modules. API has reports moderation endpoints; full CRUD screens still need live API-backed forms. |
| `/status` | Builds successfully. | Responsive build passes. | Status page still needs deployed backend URL to show live API state. |
| `/company`, `/about`, `/contact`, `/sources`, `/roadmap`, `/partners`, `/affiliate-program`, `/api-access`, `/careers` | Build successfully. | Responsive build passes. | Informational pages render; no unfinished destructive controls found. |
| `/legal/[slug]` | Legal pages build. | Responsive build passes. | Static legal content renders. |
| `/articles`, `/articles/[slug]` | Article index and brand articles build. | Responsive build passes. | Article routes render for brand/entity SEO. |
| `/robots.txt`, `/sitemap.xml`, `/manifest.webmanifest` | Generated by Next.js. | N/A | SEO artifacts are generated in build output. |
| `/api/health` on Express API | Implemented. | N/A | Returns `{ "status": "ok", "service": "myfundedscope-api" }`. |
| `/api/persistence/*` on Express API | Implemented. | N/A | Requires Bearer token; validates input; writes authenticated user-owned persistence rows. |
| `/api/admin/reports` on Express API | Implemented. | N/A | Requires authenticated admin role; lists and updates moderation reports. |

## Known QA limits

- Production Supabase migrations were not applied from this environment.
- Supabase email verification and password reset delivery cannot be confirmed without live Supabase Auth SMTP/settings.
- Cross-user RLS enforcement must be verified against the real Supabase project after migrations are applied.
- Screenshots were not captured in this pass because the request was implemented and verified by build/typecheck, not by launching a browser session against production.
