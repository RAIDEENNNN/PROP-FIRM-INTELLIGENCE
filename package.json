{
  "name": "fundscope-prop-firm-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Prop Firm Intelligence Platform with comparison, coupons, giveaways, dashboard tracking and API foundation.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check app.js && node --check storage.js && node --check scripts/migrate-json-to-postgres.js",
    "db:migrate": "node scripts/migrate-json-to-postgres.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
